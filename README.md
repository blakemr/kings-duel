# Kings Duel: a chess bot tournament simulator

The purpose of this project is to simulate chess bot tournaments. more to come.

## Setup
Make sure to install the required packages using 
```
pip install -r requirements.txt
```